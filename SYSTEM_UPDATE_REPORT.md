## تقرير تطبيق النظام الجديد للأبواب والتخصيص المالي

### المشكلة السابقة:
- الأبواب مربوطة بنوع التمويل (سنوي/شهري) عند الإنشاء
- كل باب لا يمكن استخدامه إلا لنوع تمويل واحد
- ضياع وقت في إعادة إنشاء نفس الأبواب لأنواع تمويل مختلفة

### الحل الجديد المطبق:

#### 1. **فصل تعريف الأبواب عن نوع التمويل**
```dart
/// نموذج الباب التمويلي (الرئيسي أو الفرعي) - بدون ربط بنوع التمويل
@collection
class FundingCategory {
  Id id = Isar.autoIncrement;
  late String name;
  int? parentId;
  String? description;
  DateTime? createdAt;
  DateTime? updatedAt;
}
```

#### 2. **نقل معلومات التمويل إلى التخصيص**
```dart
/// نموذج ربط المؤسسة بالباب التمويلي - يحتوي على معلومات التمويل
@collection
class InstitutionFunding {
  Id id = Isar.autoIncrement;
  late int institutionId;
  late int categoryId;
  late String fundingType; // 'سنوي' أو 'شهري' - تم نقله هنا
  late double allocatedAmount;
  late double reservedAmount;
  late double spentAmount;
  late int year; // السنة - تم نقله هنا
  int? month; // الشهر (للتمويل الشهري) - تم نقله هنا
  // ...
}
```

#### 3. **تحديث منطق التخصيص**
- عند التخصيص، نختار الباب ونحدد نوع التمويل والسنة والشهر
- نفس الباب يمكن أن يكون له تخصيصات متعددة:
  - تخصيص سنوي لعام 2025
  - تخصيص شهري لعام 2025 - يناير
  - تخصيص شهري لعام 2025 - فبراير
  - إلخ...

#### 4. **البيانات التجريبية المحدثة**
```dart
// فئات بدون ربط بنوع التمويل
final cat1 = FundingCategory()
  ..name = 'الرواتب والأجور'
  ..description = 'رواتب الموظفين والأجور الإضافية'
  ..parentId = null;

final cat2 = FundingCategory()
  ..name = 'المستلزمات المكتبية'
  ..description = 'القرطاسية والمواد المكتبية'
  ..parentId = null;
```

### المزايا الجديدة:

1. **مرونة في استخدام الأبواب**
   - الباب الواحد يمكن استخدامه لتمويل سنوي وشهري معاً
   - لا حاجة لإعادة إنشاء الأبواب

2. **سهولة إدارة الأبواب**
   - إضافة الأبواب تصبح أبسط (اسم + وصف + باب أعلى)
   - لا حاجة لتحديد نوع التمويل عند إنشاء الباب

3. **دقة في التتبع**
   - كل تخصيص يحتوي على معلومات التمويل الخاصة به
   - يمكن تتبع نفس الباب عبر فترات مختلفة

4. **توفير الوقت**
   - لا حاجة لإعادة إنشاء الأبواب لكل نوع تمويل
   - إدارة أفضل للهيكل الهرمي

### الملفات المحدثة:

1. **`lib/models/funding_models.dart`**
   - تحديث `FundingCategory`: إزالة `fundingType`, `year`, `month`
   - تحديث `InstitutionFunding`: إضافة `fundingType`, تحديث `month` ليكون اختياري

2. **`lib/services/database_service.dart`**
   - تحديث `createSampleFundingCategories()` لإنشاء أبواب عامة

3. **`lib/screens/funding_allocation_screen.dart`**
   - تحديث `_getCurrentLevelCategories()` لعرض جميع الأبواب
   - تحديث `_allocateFunding()` لحفظ معلومات التمويل في التخصيص

### خطوات الاستخدام الجديدة:

1. **إضافة الأبواب** (مرة واحدة فقط):
   - اذهب إلى "إدارة الأبواب"
   - أضف الأبواب الرئيسية (مثل: الرواتب والأجور)
   - أضف الأبواب الفرعية (مثل: رواتب الموظفين، أجور إضافية)

2. **التخصيص المالي**:
   - اذهب إلى "التخصيص المالي"
   - اختر الباب من القائمة
   - حدد نوع التمويل: سنوي أو شهري
   - حدد السنة
   - حدد الشهر (للتمويل الشهري)
   - أدخل المبلغ
   - اضغط "تخصيص"

### النتيجة:
الآن يمكن للباب الواحد (مثل "الوقود والمحروقات") أن يكون له:
- تخصيص سنوي: 1,000,000 د.ع لعام 2025
- تخصيص شهري يناير: 100,000 د.ع لعام 2025
- تخصيص شهري فبراير: 120,000 د.ع لعام 2025
- وهكذا...

دون الحاجة لإنشاء أبواب منفصلة لكل نوع تمويل!